# ParcelPilot AI Support System — direct dependencies.
# Pinned to the versions the committed database and Chroma collections were
# built against. See docs/ARCHITECTURE.md §2 for why each of these was chosen.

# --- Agent orchestration (D1: LangGraph for interrupt() + durable checkpointer) ---
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.6.0
# One OpenAI-compatible client serves both providers via base_url (D9a).
langchain-openai==1.6.0

# --- Backend (D18: FastAPI + Streamlit over SSE) ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
sse-starlette==3.4.8
itsdangerous==2.2.0          # signed session tokens (D17)

# --- Retrieval (D3/D20: Chroma Cloud + local; D4: hybrid BM25 + dense) ---
chromadb==1.5.9
rank-bm25==0.2.2

# --- Build-time ingestion (PDF -> clause registry + chunks; xlsx -> SQLite) ---
pypdf==6.16.1
openpyxl==3.1.5

# --- Model layer (OpenAI-compatible wire format for both providers) ---
# ragas -> instructor caps openai below 3.x. One venv is worth more to a reviewer
# than being on the newest client, and we only use chat.completions + embeddings,
# which are stable across 2.x and 3.x.
openai==2.54.0
httpx==0.28.1
tenacity==9.1.4

# --- Interface ---
streamlit==1.62.0

# Browser tests (`-m ui`). Not needed to run the app; needed to test the
# client the way a person uses it.
playwright==1.58.0
pytest-playwright==0.7.2

# --- Config / validation ---
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
PyYAML==6.0.3
tzdata==2026.3               # Asia/Kolkata must resolve on any host (D6)

# --- Dev / test ---
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
ruff==0.16.4

# --- Evaluation (D21, layer 3) ---
ragas==0.4.3
datasets==5.0.1
